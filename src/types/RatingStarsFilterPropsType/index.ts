// types
import { QueryConfigType } from "src/types";

interface RatingStarsFilterPropsType {
	queryConfig: QueryConfigType;
	mobileResponsive?: boolean;
	t?: string;
}
export default RatingStarsFilterPropsType;
