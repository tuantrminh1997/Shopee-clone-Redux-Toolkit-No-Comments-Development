// - tạo 1 event target:
// EventTarget = 1 Api có sẵn, hầu như mọi trình duyệt đều hỗ trợ -> ta cứ yên tâm sử dụng.
const clearLocalStorageEventTarget = new EventTarget();
export default clearLocalStorageEventTarget;
